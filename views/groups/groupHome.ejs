<head>
    <title><%= group.name &></title>
</head>
<body>
    <header>
        <div class="banner">
            <img class="group-photo" src="<% = group.group_photo %>" alt="<%= group.name %>">
            <h1><% = group.name %></h1>
            <p><%= group.member_count %> followers</p>
        </div>
    </header>
    <main>
        <div class="channels">
            <h2>Channels</h2>
            <ul>
                <% group.channels.forEach(function(channel) { %>
                    <li><%= channel %></li>
                <% }); %>
            </ul>
        </div>
    </main>
</body>
